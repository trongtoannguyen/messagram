spring.application.name=identity-service
server.port=8080

# Comma-separated allowlist emails for invite-only registration
identity.allowlist.enabled=true
identity.allowlist.emails=founder@example.com,user@example.com
